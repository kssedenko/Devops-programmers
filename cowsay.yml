---
- hosts: all
  tasks:
    - name: update apt cache
      apt:
        update_cache: true
        cache_valid_time: 3600

    - name: install cowsay
      apt:
        pkg: cowsay
        state: present
      tags: pkg_cowsay
